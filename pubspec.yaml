name: arb_excel
description: Generates ARB files from an Excel file or create an Excel file from ARB files.
version: 0.1.0
homepage: https://github.com/ez-connect/arb_excel

environment:
  sdk: '^3.6.0'
  flutter: '>=3.27.0'

dependencies:
  args: ^2.6.0
  excel: ^4.0.6
  path: ^1.8.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.14
